#include <stdio.h>
int main() 
{
   int n = 3;
   int sum = 0;
   for (int i = 1; i <= n; i++)
      sum += 2*n*(n+1)*(2*n+1)/3;
   printf("The sum of square of first %d odd numbers is %d",n, sum);
   return 0;
}
